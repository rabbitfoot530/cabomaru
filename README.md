# Cabomaru: Tokenizer for japanese sentence.
![Python Versions](https://img.shields.io/badge/Python-3.11%20|%203.12%20|%203.13%20|%203.14-blue)
![OS Support](https://img.shields.io/badge/OS-macOS%2011%2B%20%7C%20Linux-blue)

Cabomaru（かぼまる）は、日本語のテキストに対するトークナイザです。教師なし学習によりモデル作成を行い、トークナイズします。

## インストール
環境に合わせたversionをインストールします。

```sh
(例) v0.0.1 for python3.14 on macOS(Apple Silicon)
pip install https://github.com/rabbitfoot530/cabomaru/releases/download/v0.0.1/cabomaru-0.0.1-cp314-cp314-macosx_11_0_arm64.whl
```

## 学習用テキストデータフォーマット
学習用テキストデータをファイルに1行単位で記述します。複数のファイルを1つのディレクトリに格納し、ディレクトリ配下のファイルを全て学習時の対象ファイルとすることが可能です。テキストファイルのフォーマットは、以下のように1行単位にします。

```txt:training-data.txt
本書は初歩的な線形代数のみで通読できるという前書きの裏切り確率を求める
行間が広い本を読み切る精神力が欲しい
それってChatGPTに聞いただけですよね
```

## モデル作成
モデル作成には、以下のコマンドによる操作を行います。

```sh
-t: train mode
-i: 入力ファイル or 入力ディレクトリ
-o: モデル出力パス
-m: 既存モデルファイル

(例) 新規学習（ディレクトリ指定）
cabomaru -t -i "./path/to/data/" -o "./path/to/model.bin"

(例) 新規学習（ファイル指定）
cabomaru -t -i "./path/to/data/sample.txt" -o "./path/to/model.bin"

(例) 追加学習（ディレクトリ指定）
cabomaru -t -i "./path/to/data/" -o "./path/to/new_model.bin" -m "./path/to/model.bin"
```

## ユーザ定義モデル作成
テキストファイルに1行単位で定義したユーザ定義トークンをトークナイズ時のトークンとして登録可能です。

```txt:user-data.txt
本日は晴天なり
明日は明日の風が吹く
今日の天気
3.14
```

ユーザ定義ビルドでは、ユーザ定義トークンのファイルと、ユーザ定義モデルの出力パスを指定します。

```sh
-b: build mode
-i: 入力ファイル
-o: 出力ファイル

(例) user.txtの内容をuser.binへ変換
cabomaru -b -i "./path/to/user.txt" -o "./path/to/user.bin"
```

## 設定ファイル
モデル・ユーザ定義モデルのパスを設定ファイルとして設定可能です。以下の内容をcabomaru.toml(任意の名称のtomlファイル)として保存し、起動時に指定可能です。

```
model_path: 必須
user_defined_path: オプション
```

```toml
model_path = "/path/to/model.bin"
user_defined_path = "/path/to/user.bin"
```

## トークナイズサンプル
```sh
(例) ユーザ定義なし
> cabomaru -m "./path/to/model.bin"
cabomaru🌴> 残りの人生、今日が一番若い
["残りの", "人生", "、今日が", "一番", "若い"]
cabomaru🌴> 円周率は3.14です
["円周", "率は", "3", ".", "14", "です"]

(例) ユーザ定義あり
> cabomaru -m "./path/to/model.bin" -u "./path/to/user.bin"
cabomaru🌴> 円周率は3.14です
["円周", "率は", "3.14", "です"]

(例) 設定ファイル使用
> cabomaru -c "./cabomaru.toml"
cabomaru🌴> 円周率は3.14です
["円周", "率は", "3.14", "です"]
```

## ライブラリの使用方法
Pythonのライブラリとして使用するには、モデルのパスを指定した設定ファイルを指定します。

```py
import cabomaru

tokenizer = cabomaru.Tokenizer(config_path="./cabomaru.toml")

text = "円周率は3.14です"
parsed = tokenizer.parse(text)
print(parsed)
```